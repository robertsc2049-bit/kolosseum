2026-02-06: package-lock.json changed due to adding workspace package @kolosseum/engine (file:engine).
Command: npm install
Intentional: yes (to consume engine via package exports/types instead of engine/src).
Risk: low; local workspace link only; no new runtime deps introduced.
2026-02-06: Added workspace package @kolosseum/engine (file:engine) so API consumes engine via package exports/types (no engine/src imports).
2026-02-06: Added workspace package @kolosseum/engine (file:engine) so API consumes engine via package exports/types (no engine/src imports).
