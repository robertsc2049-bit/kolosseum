server.pid
server.log# local env
.env

# build + deps
dist/
node_modules/
public/app/react-dist/

# local scratch outputs
_tmp_pipeline_out.json
tmp_planned_session.json
# local env
.env

# build + deps
dist/
node_modules/
public/app/react-dist/

# local scratch outputs
_tmp_pipeline_out.json
tmp_planned_session.json
# generic temp
_tmp*
tmp_*
*.tmp
server.pid
server.log

# banned footgun
scripts/engine-status.ps1

out/

# local debug scratch
tmp/

engine.run.log
verify.full.log
*.run.log
*.full.log

# local smoke-run logs
.logs/

# message attachment storage (local disk, dev/runtime only)
var/

# local Claude Code tool state (per-machine, never shared)
.claude/
