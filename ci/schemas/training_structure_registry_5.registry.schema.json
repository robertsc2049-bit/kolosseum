{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kolosseum://registries/canonical/training_structure_registry_5.registry.schema.json",
  "title": "training_structure_registry_5",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "registry_id",
    "version",
    "entries"
  ],
  "properties": {
    "registry_id": {
      "type": "string",
      "const": "training_structure_registry_5"
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "entries": {
      "type": "array",
      "maxItems": 0,
      "items": false
    }
  },
  "description": "REG-FULL-01 hard-closes rows because the supplied authority defines token categories but not a single canonical row field list."
}
